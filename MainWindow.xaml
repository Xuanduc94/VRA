<Window x:Class="Viettel_Report_Automation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Viettel_Report_Automation"
        mc:Ignorable="d"
        SizeToContent="WidthAndHeight"
        Title="Tự động hoá báo cáo" MaxHeight="300" MaxWidth="600" WindowStartupLocation="CenterScreen">
    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Menu Background="LightGray">
                <MenuItem Header="Trợ giúp" x:Name="btnHelp" Click="btnHelp_Click" />
            </Menu>
        </StackPanel>
        <StackPanel  Orientation="Horizontal" x:Name="excel_area">
            <Label Width="150" Content="File Excel KPI tháng "/>
            <Label Style="{StaticResource labelNormal}" Width="100" x:Name="lbl_fileName_excel" Content="File được chọn"/>
            <Button Style="{StaticResource NormalButton}" x:Name="btn_openFileExcel" Click="btn_openFileExcel_Click"  Height="30" Width="100" Content="Chọn file" Margin="10,10,90,10" Grid.ColumnSpan="2"/>
        </StackPanel>
        <StackPanel IsEnabled="False" x:Name="kpi_area" Orientation="Horizontal">
            <Label Width="150" Content="File Chấm điểm"/>
            <Label Style="{StaticResource labelNormal}" Width="100" x:Name="lbl_chamdiem" Content="File được chọn"/>
            <Button Style="{StaticResource NormalButton}" x:Name="btn_chamdiemkpi" Click="btn_chamdiemkpi_Click" Height="30" Width="100" Content="Chọn file" Margin="10,10,90,10" />
        </StackPanel>
       <!--
        <StackPanel IsEnabled="False" x:Name="word_are" Orientation="Horizontal">
            <Label Width="150" Content="File Word"/>
            <Label Style="{StaticResource labelNormal}" Width="100" x:Name="lbl_fileName" Content="File được chọn"/>
            <Button Style="{StaticResource NormalButton}" x:Name="btn_openFile" Click="btn_openFile_Click" Height="30" Width="100" Content="Chọn file" Margin="10,10,90,10" Grid.ColumnSpan="2"/>
        </StackPanel>
        --> 
        
        <StackPanel>
            <StackPanel.Resources>
                <Style TargetType="Label">
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                </Style>
            </StackPanel.Resources>
        </StackPanel>
        <Label x:Name="lblProcess" HorizontalContentAlignment="Center"/>
      
        <Button Style="{StaticResource PrimaryButton}" x:Name="btnExtract" Click="btnExtract_Click" Content="Trích xuất dữ liệu"/>
    </StackPanel>
</Window>
